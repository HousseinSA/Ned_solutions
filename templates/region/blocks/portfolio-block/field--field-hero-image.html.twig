  <div class="content" >
    <div class="content-1"  >
     
      {% for item in items %}
      {% set image = item.content['#media'] %}
      {% set image_uri = image.field_media_image.entity.uri.value %}
      <a href="{{ file_url(image_uri) }}" title="project info here">
        <img src="{{ file_url(image_uri) }}" alt="example-1"  class="outer img-responsive slideanim" />
      </a>
      {% endfor %}
      
    </div>
    <div class="content-2" >
     
      {% for item in items %}
      {% set image = item.content['#media'] %}
      {% set image_uri = image.field_media_image.entity.uri.value %}
      <a href="{{ file_url(image_uri) }}" title="project info here">
        <img src="{{ file_url(image_uri) }}" alt="example-2" class="outer img-responsive slideanim" />
      </a>
      {% endfor %}
      
    </div>

    <div class="content-3" >
     
      {% for item in items %}
      {% set image = item.content['#media'] %}
      {% set image_uri = image.field_media_image.entity.uri.value %}
      <a href="{{ file_url(image_uri) }}" title="project info here">
        <img src="{{ file_url(image_uri) }}" alt="example-3" class="outer img-responsive slideanim" />
      </a>
      {% endfor %}
      
    </div>
    <div class="content-4" >
     
      {% for item in items %}
      {% set image = item.content['#media'] %}
      {% set image_uri = image.field_media_image.entity.uri.value %}
      <a href="{{ file_url(image_uri) }}" title="project info here">
        <img src="{{ file_url(image_uri) }}" alt="example-4" class="outer img-responsive slideanim" />
      </a>
      {% endfor %}
      
    </div>
  </div>

